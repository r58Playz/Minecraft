cmake_minimum_required(VERSION 2.8.12)
project(utils)
set(SOURCE_DIR "mc/")
include_directories(${SOURCE_DIR})
set(SOURCES "${SOURCE_DIR}/utils.cpp")
add_subdirectory(lib/pybind11)
pybind11_add_module(utils ${SOURCES} "${SOURCE_DIR}/bindings.cpp")
